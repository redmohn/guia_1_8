CREATE TABLE `guia_1_8_Taller`.`instrumento` (
  `codigo` VARCHAR(255) NOT NULL,
  `nombre` VARCHAR(255) NOT NULL,
  `stock` INT NOT NULL,
  `tipo` VARCHAR(255) NOT NULL,
  PRIMARY KEY (`codigo`))
DEFAULT CHARACTER SET = utf8;

